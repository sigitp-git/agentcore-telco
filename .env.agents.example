# Agent Configuration Environment Variables - Example Template
# Copy this file to .env.agents and replace placeholder values with your actual configuration
# 
# SECURITY NOTE: Never commit .env.agents with real values to version control
# Add .env.agents to your .gitignore file

# =============================================================================
# EKS Agent Configuration
# =============================================================================
# Amazon EKS and Kubernetes container orchestration specialist
EKS_AGENT_RUNTIME_ARN=arn:aws:bedrock-agentcore:REGION:ACCOUNT_ID:runtime/eks_agent-RUNTIME_ID
EKS_AGENT_GATEWAY_ID=eks-agent-agentcore-gw-GATEWAY_ID

# =============================================================================
# VPC Agent Configuration  
# =============================================================================
# AWS networking and connectivity specialist
VPC_AGENT_RUNTIME_ARN=arn:aws:bedrock-agentcore:REGION:ACCOUNT_ID:runtime/vpc_agent-RUNTIME_ID
VPC_AGENT_GATEWAY_ID=vpc-agent-agentcore-gw-GATEWAY_ID

# =============================================================================
# Outposts Agent Configuration
# =============================================================================
# AWS hybrid cloud and edge computing specialist
OUTPOSTS_AGENT_RUNTIME_ARN=arn:aws:bedrock-agentcore:REGION:ACCOUNT_ID:runtime/outposts_agent-RUNTIME_ID
OUTPOSTS_AGENT_GATEWAY_ID=outposts-agent-agentcore-gw-GATEWAY_ID

# =============================================================================
# Prometheus Agent Configuration
# =============================================================================
# Monitoring and observability specialist
PROMETHEUS_AGENT_RUNTIME_ARN=arn:aws:bedrock-agentcore:REGION:ACCOUNT_ID:runtime/prometheus_agent-RUNTIME_ID
PROMETHEUS_AGENT_GATEWAY_ID=prometheus-agent-agentcore-gw-GATEWAY_ID

# =============================================================================
# Optional: AWS Configuration Override
# =============================================================================
# Uncomment and set if you need to override default AWS settings
# AWS_DEFAULT_REGION=us-east-1
# AWS_PROFILE=your-aws-profile

# =============================================================================
# Setup Instructions
# =============================================================================
# 1. Copy this file: cp .env.agents.example .env.agents
# 2. Replace placeholder values:
#    - REGION: Your AWS region (e.g., us-east-1, us-west-2)
#    - ACCOUNT_ID: Your 12-digit AWS account ID
#    - RUNTIME_ID: The unique runtime identifier from your AgentCore deployment
#    - GATEWAY_ID: The unique gateway identifier from your AgentCore setup
# 3. Ensure .env.agents is in your .gitignore file
# 4. Set appropriate file permissions: chmod 600 .env.agents

# =============================================================================
# Example Values (DO NOT USE IN PRODUCTION)
# =============================================================================
# EKS_AGENT_RUNTIME_ARN=arn:aws:bedrock-agentcore:us-east-1:123456789012:runtime/eks_agent-abc123def456
# EKS_AGENT_GATEWAY_ID=eks-agent-agentcore-gw-xyz789abc123